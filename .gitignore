# Build toolchain (downloaded locally, not committed)
tools/
local.properties

# Build outputs
build/
**/build/
.gradle/

# ...but keep electron-builder resources (icons) tracked
!desktop/build/

# IDE
.idea/
*.iml

# Desktop (Electron) build artifacts
desktop/node_modules/
desktop/dist/

# Agent session data & phone screenshots (may contain credentials)
.cam-images/
.agents/
